<?xml version="1.0" encoding="utf-8"?>
<lint>
    <!--
      Dependencies are intentionally held at the exact versions covered by the
      Windows/Linux/Room migration gates. Dependabot reports upgrades separately;
      library modernization is not mixed into a release-candidate hardening change.
    -->
    <issue id="GradleDependency" severity="ignore" />

    <!--
      AGP 9 built-in Kotlin is enabled. Room remains on the official
      com.android.legacy-kapt compatibility path for this release because a KSP
      migration changes the compiler pipeline and requires an independent migration PR.
      Exact Linux, Windows, Room-schema, and installed-APK gates cover this exception.
    -->
    <issue id="KaptUsageInsteadOfKsp" severity="ignore" />
</lint>
